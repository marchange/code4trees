name: 🚀 Deploy to IONOS Webspace

on:
  push:
    branches:
      - main
      - dev

jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0 
    
    - name: 📂 Mirror files via SFTP (Upload & Delete)
      uses: pressidium/lftp-mirror-action@v1
      with:
        host: ${{ secrets.SFTP_SERVER }}
        user: ${{ secrets.SFTP_USERNAME }}
        pass: ${{ secrets.SFTP_PASSWORD }}
        port: 22
        localDir: './public'
        #Deploy to /public if main branch, otherwise deploy to /dev
        remoteDir: ${{ github.ref_name == 'main' && '/public' || '/dev' }}
        reverse: true
        options: '--delete --exclude-glob .htaccess --exclude-glob .htpasswd'